#include "RayTracer.h"

RayTracer::RayTracer(const Scene& scene)
	:m_Scene(scene) {}

void RayTracer::render()
{
	// Generate rays and loop through them.
	// In that loop loop through all objects to check for intersection and draw acordingly
}
